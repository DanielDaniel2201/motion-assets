# Motion Assets 项目说明

## 项目目标

本项目是一个面向内容创作者的 Web 动效素材制作工具。

用户在网页中上传图片、文字、视频片段或其他原始素材，选择并调整动效，然后在浏览器中完成预览、合成与导出。最终产物是带真实透明背景（Alpha 通道）的动态素材，主要导出格式为 MOV，供用户继续导入剪映等视频编辑软件进行内容创作。

## 核心使用流程

1. 用户打开网页并选择一种 Motion Asset 模板或效果。
2. 用户上传原始素材并调整文字、尺寸、颜色、时间、动效等参数。
3. 网页实时预览最终动画以及透明区域。
4. 浏览器在本地完成素材处理和渲染。
5. 用户下载带透明背景的 MOV 文件。
6. 下载结果应能直接导入剪映，并作为透明背景的动效叠加素材使用。

## 不可妥协的产品约束

- 所有用户素材与渲染处理默认只发生在浏览器本地，不上传到业务服务器。
- 不依赖用户本地安装 Node.js、FFmpeg 或其他桌面软件；用户通过浏览器即可完成完整流程。
- “透明背景”必须是导出文件中的真实 Alpha 通道，不能只是黑底、绿幕或棋盘格预览效果。
- 不能仅凭网页预览正常就宣称功能完成；必须验证实际下载文件的透明通道、时长、尺寸、帧率和播放结果。
- MOV 导出能力必须以剪映实际导入测试为最终兼容性验收依据。
- 处理失败时应保留用户当前编辑状态，并给出清楚、可执行的错误提示。
- 对大文件、内存不足、浏览器不兼容和导出耗时等情况，要有明确限制、进度反馈与降级说明。

## 技术与架构方向

- 项目形态是可直接在 Chrome 等现代浏览器中使用的 Web Page。
- 计划部署到 Cloudflare；前端页面、静态资源和必要的边缘能力应适配 Cloudflare 的部署环境。
- 核心素材处理、动画预览与文件导出应优先使用浏览器端能力，包括 Canvas、WebGL、WebAssembly、Web Workers、WebCodecs 等合适技术。
- 重计算任务应尽量离开主线程，避免导出期间页面失去响应。
- 在引入第三方编码器、WASM 包或媒体库前，应评估浏览器兼容性、包体积、启动时间、许可证和 Cloudflare 部署限制。
- 不把服务端转码作为默认实现；如未来确需服务端能力，必须先明确隐私、成本和产品边界，不能悄然改变“网页本地处理”的承诺。

## 参考项目

本仓库下的 `sticker-forge/` 是浏览器本地处理体验和工程实现的参考项目，对应开源项目：

- 在线体验：https://sticker.oooo.so/
- GitHub：https://github.com/CatsJuice/sticker-forge

开发时重点参考它的以下做法：

- 用户素材在浏览器本地处理。
- WebGL/Canvas 驱动的实时交互与预览。
- 复杂计算与交互反馈之间的性能平衡。
- 导出流程、进度反馈、移动端适配和浏览器兼容性处理。

除非任务明确要求，不直接修改 `sticker-forge/`；新的 Motion Assets 产品代码应与参考仓库保持清晰边界。

## 开发与验收原则

- 先建立一个最小但完整的端到端流程：上传素材 → 预览动效 → 浏览器本地渲染 → 下载 MOV → 剪映导入验证。
- 每种动效资产都应有明确的输入要求、可调参数、预览结果和导出规格。
- 预览与导出应共享同一套时间、布局和动画定义，避免“网页看到的”和“下载得到的”不一致。
- 涉及媒体格式或透明通道的实现，必须用真实文件探测结果和剪映实测结果说明是否完成。
- 原型、静态预览、无透明通道的视频或尚未经过剪映验证的文件，不得描述为已完成的可交付 Motion Asset。
- 优先进行非破坏性修改，保留用户素材和已有项目结构；不要覆盖用户文件。

## 首要验收标准

首个可交付版本至少需要满足：

1. 用户无需安装本地软件即可在网页完成操作。
2. 原始素材不离开浏览器。
3. 网页预览与导出动画在构图、时间和运动上保持一致。
4. 下载得到的 MOV 含可验证的真实 Alpha 通道。
5. MOV 可成功导入目标版本的剪映，并正确显示透明背景。
6. Cloudflare 上的正式网页可直接访问并完成上述全流程。
